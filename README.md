# Chat-with-SQL-DB
A conversational SQL agent built with LangChain, Llama 3, and Streamlit that translates plain English questions into SQL queries, executes them in real time, and returns natural language answers — with full ReAct reasoning streamed live to the UI.
